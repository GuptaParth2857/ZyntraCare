name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ZyntraCare/package-lock.json

      - name: Install dependencies
        run: cd ZyntraCare && npm ci

      - name: Build web app
        run: cd ZyntraCare && npm run build
        env:
          DATABASE_URL: "file:./dev.db"
          NEXTAUTH_SECRET: "dummy_secret_for_build"

      - name: Sync Capacitor Android
        run: cd ZyntraCare && npx cap sync android

      - name: Build Android APK
        run: |
          cd ZyntraCare/android/App
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ZyntraCare-debug.apk
          path: ZyntraCare/android/App/build/outputs/apk/debug/App-debug.apk